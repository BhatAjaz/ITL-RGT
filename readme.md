When generative models train on data produced by earlier generative models,1
quality degrades—a phenomenon known as model collapse. But why does collapse2
occur, how fast does it proceed, and can anything stop it?
